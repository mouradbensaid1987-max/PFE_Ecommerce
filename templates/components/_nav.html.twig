<nav class="navbar navbar-expand-lg navbar-dark bg-primary"  data-bs-theme="dark">

    <div class="container">

        <a class="navbar-brand fw-bold" href="{{ path('app_home') }}">
              <img src="{{ asset('images/logo_nav.svg') }}" alt="" height="40" >
              FixPro
        </a>

        <div class="collapse navbar-collapse" id="navMain">
            <ul class="navbar-nav me-auto">
                <li class="nav-item">
                    <a class="nav-link text-white" href="{{ path('app_product_list') }}">Produits</a>
                </li>

                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle text-white" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        Catégories
                    </a>
                    <ul class="dropdown-menu">
                        {% for cat in category_repository.findAll() %}
                            <li>
                                <a class="dropdown-item" href="{{ path('app_category_show', {id: cat.id}) }}"> {{ cat.name }}</a>
                            </li>
                        {% endfor %}
                    </ul>
                </li>
                {% if is_granted('ROLE_ADMIN') %}
                    <li class="nav-item">
                        <a class="nav-link text-white" href="{{ path('app_admin_dashboard') }}">Administration</a>
                    </li>
                {% endif %}


            </ul>

            <ul class="navbar-nav">
                {% if app.user %}
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle text-white" href="#" role="button"
                           data-bs-toggle="dropdown" aria-expanded="false">
                            <span aria-hidden="true">👤</span> {{ app.user.firstName }}
                        </a>
                        <ul class="dropdown-menu dropdown-menu-end">
                            <li><a class="dropdown-item" href="{{ path('app_profile') }}"><span aria-hidden="true">👤</span> Mon profil</a></li>
                            <li><a class="dropdown-item" href="{{ path('app_address_list') }}"><span aria-hidden="true">📍</span> Mes adresses</a></li>
                            <li><a class="dropdown-item" href="{{ path('app_order_history') }}"><span aria-hidden="true">📦</span> Mes commandes</a></li>
                            <li><a class="dropdown-item" href="{{ path('app_favorites') }}"><span aria-hidden="true">❤️</span> Mes favoris</a></li>
                            <li><a class="dropdown-item" href="{{ path('app_message_index') }}"><span aria-hidden="true">✉️</span> Mes messages</a></li>
  
                            <li><hr class="dropdown-divider"></li>
                            <li><a class="dropdown-item" href="{{ path('app_logout') }}">Se déconnecter</a></li>
                        </ul>
                    </li>
                {% else %}
                    <li class="nav-item">
                        <a class="nav-link text-white" href="{{ path('app_login') }}">Connexion</a>
                    </li>
                    <li class="nav-item">
                        <a class="btn btn-light btn-sm ms-2 mt-1" href="{{ path('app_register') }}">Inscription</a>
                    </li>
                {% endif %}
            </ul>
        </div>
        <div class="nav-item d-flex align-items-center">
            <a class="btn btn-outline-light btn-sm position-relative mx-2" href="{{ path('app_cart_index') }}"
               aria-label="Voir mon panier ( {{ cart.getCount() }} article(s))">
              <span aria-hidden="true">🛒</span>
                {% if cart.getCount() > 0 %}
                    <span class="position-absolute top-0 start-100 translate-middle badge rounded-pill bg-danger">
                      {{ cart.getCount() }}
                    </span>
                {% endif %}
            </a>
            
      

            <button class="navbar-toggler mx-2" type="button"
                    data-bs-toggle="collapse" data-bs-target="#navMain"
                    aria-controls="navMain" aria-expanded="false"
                    aria-label="Ouvrir/fermer le menu de navigation">
                  <span class="navbar-toggler-icon"></span>
            </button>
      </div>

    </div>
</nav>