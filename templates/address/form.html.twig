{% extends 'base.html.twig' %}

{% block title %}{{ title }}{% endblock %}

{% block body %}

  <div class="card mt-3">
      <div class="card-body">
          {{ form_start(form) }}
              {{ form_widget(form) }}
              <button class="btn btn-primary">💾 Enregistrer</button>
              <a href="{{ path('app_address_list') }}" class="btn btn-secondary">Annuler</a>
          {{ form_end(form) }}
      </div>
  </div>

{% endblock %}