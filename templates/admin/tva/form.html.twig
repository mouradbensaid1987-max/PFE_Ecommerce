{% extends 'admin/base.html.twig' %}

{% block admin_body %}

<h1>{{ title }}</h1>

<div class="card mt-3">
    <div class="card-body">
        {{ form_start(form) }}  
              {{ form_widget(form) }}
              <button class="btn btn-primary">💾 Enregistrer</button>
              <a href="{{ path('app_admin_tva_index') }}" class="btn btn-secondary">Annuler</a>
        {{ form_end(form) }}
    </div>
</div>

{% endblock %}